apiVersion: v1
kind: Pod
metadata:
  labels:
    env: demo
  name: redis-pod
spec:
  containers:
  - image: redis
    name: redis-pod
  tolerations:
  - key: "gpu"
    operator: "Equal"
    value: "true"
    effect: "NoSchedule"